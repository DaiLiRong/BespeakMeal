commit_1:
建立db_bespeakmeal数据库，新建用户注册表tbl_user；
使用NHibernate建立数据对象持久化，进行数据库连接、查询测试。

commit_2:
在commit_1的数据库上面新建数据表tbl_order,tbl_food,tbl_foodlist,分别对应订单表，菜肴表和订单-菜肴表；
对User、Order表进行领域分析，创建数据模型，对于User表和Order表中的多对一关系编写数据属性，并在mapping中创建一对多关系；
对User表和Order表进行基本的创建、查询操作。

commit_3:
修改了数据表的某些结构和名字，tbl_order中加入了status字段，以区分历史订单、新订单和无效订单；
编写Food表和OrderFood表的数据模型；
对Food表和OrderFood表进行基本的测试。

commit_4:
开始了界面的设计，在考虑用web做还是窗口做；
对用户购买食物的操作进行了逻辑层的代码编写，并完成了测试；
完成了食物列表的显示、用户将食物放入购物车中等功能。

commit_5:
完成了购物车的显示；
完成了用户资料的显示和修改。

commit_6:
完成了订单的生成；
完成了用户查看订单的页面，订单的详细内容要在下一步完成。

commit_7:
用户操作：订单显示，订单状态，交易或取消订单，确认订单；
管理员：添加、显示和编辑所有食物（食物名字，图片，类别，单价，状态，交易量等）。

commit_8：
管理员操作：查看订单，打印订单。